# Building LeoMiniGames

The canonical v0.6.0 build instructions are in [`docs/BUILDING.md`](docs/BUILDING.md).

The application target is `LeoMiniGames`; built-in game plugin targets are static dependencies and should not be selected as Android deploy targets.
